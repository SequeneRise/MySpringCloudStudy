spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: sequeneRise-config
      uri: http://127.0.0.1:4396
      profile: dev
      label: master
      name: dataSource,rabbitmq
#配置注册中心
eureka:
  client:
    service-url:
      defaultZone: http://localhost:10301/eureka/

